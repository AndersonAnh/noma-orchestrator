config.stopBubbling = true

# Без этого @RequiredArgsConstructor и @Qualifier не дружат друг с другом
# Если ошибка продолжает появляться, рекомендую сделать Rebuild
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier